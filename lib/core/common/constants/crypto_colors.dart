import 'package:flutter/material.dart';

class CColors {
  // App theme colors
  static const Color primary = Color(0xff000000);
  static const Color secondary = Color.fromARGB(103, 29, 29, 215);

  // Text colors
  static const Color primaryTextColor = Color(0xff000000);
  static const Color secondaryTextColor = Color(0xff575757);

  // Background colors
  static const Color backgroundColor = Color.fromARGB(214, 248, 248, 248);

  // Border colors
  static const Color border = Color(0xffdfdfdf);

  // Neutral Shades
  static const Color white = Color(0xffffffff);
  static const Color grey = Color(0xfff5f5f5);
  static const Color darkgrey = Color(0xff808080);
}
